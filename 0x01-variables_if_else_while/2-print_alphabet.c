#include <stdio.h>

 /**
  * main - print alpha in lower case.
  * Return: Always (0)
  */
int main(void)
{
	char lc;

	for (lc = 'a'; ls <= 'z' lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
